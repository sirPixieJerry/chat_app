// component to show and update the bio of the user
export default function Bio({}) {
    return (
        <>
            <p></p>
            <form onSubmit={}>
                <textarea></textarea>
                <button>EDIT BIO</button>
            </form>
        </>
    );
}
